"use strict";
{
    const numberFunction = (param1, param2) => {
        let sum = param1 + param2;
        return sum;
    };
    const result = numberFunction(2, 5);
    console.log(result);
    //
}
